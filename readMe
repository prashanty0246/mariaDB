export const keepConnectionAlive = async () => {
  try {
    const isInitialized = AppDataSource.isInitialized;

    if (!isInitialized) {
      await AppDataSource.initialize();
    }

    return true;
  } catch (error) {
    return false;
  }
};
